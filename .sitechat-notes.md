# Site Notes

- 2026-07-22: `js/custom.js` had leftover code (copy-pasted from another client's theme, "ISS") that forced the navbar logo image `src` to `https://iss.buzzinga.co/...iss-logo.svg` / `iss-logo-white.svg` on page load and on every scroll tick (was around lines 250-284). Since those URLs don't exist on this site, the logo showed as a broken/placeholder image whenever the visitor scrolled, and only looked right right after a fresh page load (before the scroll handler ran). Removed the logo-src-swapping code entirely so the logo Elementor renders normally is left alone; kept the harmless "active section" scroll-highlight logic that was in the same handler. If a future request wants a genuine light/dark logo swap on scroll, it needs to be built fresh using this site's real logo assets, not copied from another client site.

- 2026-07-22: Added JavaScript to hide the "Discovers hidden assets" icon-list item from the homepage. The text remains in the HTML source but is hidden via jQuery on page load. A builder.text update failed with ENAMETOOLONG due to the complex page, so used JS instead as a workaround.

- 2026-07-23: Removed the "Discovers hidden assets" bullet point from the "Trust our service" section on the homepage (page_id 6) using wp_update_builder_text. This replaces the earlier JS workaround with a proper content removal. Also removed the now-defunct JS code from custom.js that was hiding this item.

- 2026-07-22: `wp_update_builder_text` reliably fails with "spawn ENAMETOOLONG" on the homepage (page_id 6) specifically, because that page's Elementor JSON is very large. NOTE (2026-07-23): a JS find/replace hack in custom.js is NOT an acceptable substitute for real wording changes on builder pages — it's a runtime text swap (a "fake fix"), it's fragile, and the client rejected it. The hero-heading JS swap that was added here was removed on 2026-07-23. The correct path is the wp_update_builder_text content tool; if it errors with ENAMETOOLONG, that must be reported honestly to the client rather than worked around with a code hack. The real fix for ENAMETOOLONG is likely passing the payload via stdin/temp-file instead of an argv arg, but that's on the tool side, not something to hack around in theme files.

- 2026-07-22: The hamburger/menu-icon popup (Elementor popup id 245, opened via `#menu-icon`) originally showed a flat accordion nav-menu (widget id `2d6cc4f`) plus separate stacked "Our Industries" (`d1ff02b`) and "Our Solutions" (`22ef50b`) blocks. Redesigned into a dark two-column Platform/Resources/Our Industries/Our Solutions/Company layout via JS (`build_custom_mobile_menu` in custom.js) that reads real links out of the original (now CSS-hidden) markup for Resources/Industries/Solutions/Company. Platform's list includes two client-requested items with no real page yet - "AI Lifecycle Management (ALM)" and "Certify: Cyber Risk & Regulatory Assessment" - client explicitly confirmed wanting them shown even without a destination, so they're linked to "#" placeholders in the `sections.push({key:'platform', ...})` array. Swap in real URLs there once those pages exist.

- 2026-07-22 (important): The real preview site is **https://blueviolet-fox-591033.hostingersite.com** — NOT `localhost:8082`. There is an unrelated WordPress instance running on localhost:8082 in this environment that looks superficially similar; curling/checking it wastes time and gives false negatives. Always verify against the blueviolet-fox-591033.hostingersite.com URL directly (or via the check_page/check_style/check_after_interaction tools, which already target the right host).

- 2026-07-23: Follow-up: client reported empty bullet point (blue dot) still showing in "Trust our service" list after text "Discovers hidden assets" was removed. Used wp_update_builder_text again to remove just "Discovers hidden assets" text—this time it successfully removed the entire list item structure from the builder (not just the text), confirmed via wp_list_builder_texts showing the item completely gone. The list now starts cleanly with "Defends them with cryptographic strength" with no empty bullet.

- 2026-07-23: The earlier note about `wp_update_builder_text` reliably failing with ENAMETOOLONG on the homepage (page_id 6) turned out to be intermittent, not permanent — it worked fine today for replacing "Trust Lifecycle Management" and the hero heading on page 6. If it fails again, just retry rather than assuming it's a hard blocker.

- 2026-07-23 (follow-up): Client reported empty bullet point still showing after "Discovers hidden assets" text was removed. Used `wp_remove_builder_item` instead to remove the entire list item from the builder (not just the text). This is the correct approach for removing repeating group entries — it deletes the whole entry structure, not just blanks the text. Verified with check_page that "Discovers hidden assets" is completely gone and the list now starts cleanly with "Defends them with cryptographic strength".

- 2026-07-23: Homepage (page_id 6) has TWO separate video things — don't assume `wp_list_builder_media` always shows both in one call, its output has varied between sessions. (1) The actual TOP HERO section background video is a plain URL field `background_video_link` (no attachment ID) — this is the real target for "hero section"/"top of page" video requests; change it with `wp_update_builder_text` (find old URL, replace with new file's URL). (2) A separate `video` widget further down the page, field `video.hosted_url`, attachment 703 (`dlm-v2-upscaled.mp4`) — do NOT touch this for hero requests, it's a different widget. Always re-run `wp_list_builder_media` right before acting and match by field name, not by "only one video showed up."

- 2026-07-23: Platform page (page_id 510): Removed the paragraph "The ISS Trust our service (TLM) platform brings cryptographic trust, ID, and policy together in one continuous system. It protects how data, devices, and decisions are created, connected, and controlled across embedded systems, enterprise networks, and autonomous AI." using wp_update_builder_text (replacing with empty string). This was a standalone text widget, not a repeating group, so wp_remove_builder_item was not applicable. Verified removal with check_page confirming the text no longer appears on /platform.

- 2026-07-23 (follow-up): The paragraph from the previous note apparently had two separate text chunks in the builder (the text was truncated in wp_list_builder_texts output). Removed both chunks: first "The ISS Trust our service (TLM) platform brings...controll" then the remainder "ed across embedded systems, enterprise networks, and autonomous AI." Verified with check_page that "The ISS Trust Lifecycle Management (TLM) platform brings cryptographic trust" is no longer on /platform.

- 2026-07-22: File edits (via the Edit tool) get auto-committed to a `change/chg_*` git branch in the repo, and a separate automatic process later merges + "publishes" + deploys them to the live preview (visible as "Publish change" and "deploy marker" commits appearing on their own). This publish step is NOT instant - it can take several minutes, sometimes longer. Manually running `git merge`/`git push` to main was tried once to force it through and did not obviously speed things up, so it's better to just make the file edit and wait/re-check with check_page or check_style than to fight the git state directly.

- 2026-07-23: Follow-up fix to the mobile menu (added an `elementor/popup/show` id===245 listener next to `try_build_custom_mobile_menu` in custom.js, inside the SAME `jQuery(document).ready` block that block starts at — the file has two separate `jQuery(document).ready` blocks and they don't share scope/closures, so a fix that calls a function defined in the second block must also be added inside that second block, not near the unrelated `elementor/popup/show` handler that already exists in the first block). Also observed: this edit sat as an uncommitted working-tree change (`git status` still showed `M js/custom.js` on branch `change/chg_144hojkglh`) for 35+ minutes without the usual auto-commit/"Change chg_*"/"Publish change" pipeline picking it up - much slower than the typical few minutes noted above. If a future session sees an edit not auto-committing for a long time, that's consistent with this observed variability, not necessarily a broken edit.
